.page-header
  %h1= "Route ##{@route.trip_number}"
  %h3
    Driver:
    = link_to @route.driver.name, driver_path(@route.driver)

%table.table.table-striped
  %tbody
    -@route.attributes_to_display.each do |key, value|
      %tr
        %td= display_route_attribute(key)
        %td= route_display_value(value)
        
























-# - model_class = Route
-# .page-header
-#   %h1=t '.title', :default => model_class.model_name.human.titleize
-#
-# %p
-#   %strong= model_class.human_attribute_name(:date) + ':'
-#   %br
-#   = @route.date
-# %p
-#   %strong= model_class.human_attribute_name(:trip_number) + ':'
-#   %br
-#   = @route.trip_number
-# %p
-#   %strong= model_class.human_attribute_name(:advance) + ':'
-#   %br
-#   = humanized_money_with_symbol @route.advance
-# %p
-#   %strong= model_class.human_attribute_name(:reimbursement) + ':'
-#   %br
-#   = humanized_money_with_symbol @route.reimbursement
-# %p
-#   %strong= model_class.human_attribute_name(:gross_revenue) + ':'
-#   %br
-#   = humanized_money_with_symbol @route.gross_revenue
-# %p
-#   %strong= model_class.human_attribute_name(:net_revenue) + ':'
-#   %br
-#   = humanized_money_with_symbol @route.net_revenue
-# %p
-#   %strong Driver
-#   %br
-#   = link_to @route.driver.name, driver_path(@route.driver.id)
-# %p
-#   %strong= model_class.human_attribute_name(:trailer_rented) + ':'
-#   %br
-#   = @route.trailer_rented_str
-# %p
-#   %strong= model_class.human_attribute_name(:trailer_rental) + ':'
-#   %br
-#   = humanized_money_with_symbol @route.trailer_rental
-#
-# .form-actions
-#   = link_to t('.back', :default => t("helpers.links.back")), routes_path, :class => 'btn'
-#   = link_to t('.edit', :default => t("helpers.links.edit")), edit_route_path(@route), :class => 'btn'
-#   = link_to t('.destroy', :default => t("helpers.links.destroy")), route_path(@route), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
